init
初期化処理
フェードイン
↓
loadWait
↓if フェードイン終了
load
読込処理(非同期)
読込処理(同期処理)
ロード画面描画
if 非同期処理終了
フェードアウト
↓
StartWait
↓if フェードアウト終了
step
フェードイン
if　フェードイン終了
更新処理
↓ if 終了条件達成
endWait
フェードアウト
↓
exit
if フェードアウト終了
終了処理